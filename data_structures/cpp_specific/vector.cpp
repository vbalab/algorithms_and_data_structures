#include <vector>


int main () {
    std::vector<int> a[5];              // c array of 5 std::vectors
    // std::vector<int> b(5);              // std::vector of size 5
    // std::vector<int> c(5, -10);         // std::vector of size 5 filled with -10

    return 0;
}