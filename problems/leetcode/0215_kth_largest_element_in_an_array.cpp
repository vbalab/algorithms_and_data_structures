#include <queue>
#include <vector>
#include <iostream>


class Solution {
public:
    int findKthLargest(std::vector<int>& nums, int k) {
        std::queue<int> q;

        for (int num: nums) {
            if (q.back() < )
        }

        return q->back();
    }
};

int main() {
    return 0;
};
